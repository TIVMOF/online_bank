import "package:flutter/material.dart";

class MySend extends StatelessWidget {
  const MySend({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        leading: IconButton(icon: Icon(Icons.menu), onPressed: () {
          
        },),
      ),
    );
  }
}